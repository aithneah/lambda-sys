import React from 'react';
import "./AverageComponent.scss";

const AverageComponent = (props) =>
    <div className="averageComponentContainer">{props.average}</div>;

export default AverageComponent;